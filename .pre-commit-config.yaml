repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.20
    hooks:
      - id: ruff-check
        args: [--fix]
        files: ^backend/

      - id: ruff-format
        files: ^backend/

  - repo: local
    hooks:
      - id: backend-mypy
        name: Backend mypy
        entry: uv run --project backend mypy backend
        language: system
        pass_filenames: false

      - id: frontend-format
        name: Frontend Prettier
        entry: npm --prefix frontend run format:check
        language: system
        pass_filenames: false
        files: ^frontend/

      - id: frontend-lint
        name: Frontend Oxlint
        entry: npm --prefix frontend run lint
        language: system
        pass_filenames: false
        files: ^frontend/